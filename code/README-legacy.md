# Historical submitted implementation

This directory is retained for historical comparison. It is not the repaired
revision entry point. Use `R/difs.R`, `example/`, and `benchmark/` from the
repository root. The active benchmark sources are listed in
`provenance/source_manifest.json`; legacy identifiers may remain here.
